const initial_state = [
  {
    id: 1,
    name: "silvin",
    duration: "2 hours",
  },
];
const activitiesReducer = (state = initial_state, action) => {
  return state;
};

export default activitiesReducer;
